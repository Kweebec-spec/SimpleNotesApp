<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 2. Наведение мышкой -->
    <item android:color="?attr/colorSurfaceContainer" android:state_hovered="true" />

    <!-- 3. Фокус (когда кнопка выбрана, но не нажата) -->
    <item android:color="?attr/colorSurfaceContainer" android:state_focused="true" />

    <!-- 4. Обычное состояние -->
    <item android:color="?attr/colorSurfaceContainerLow" />

</selector>
